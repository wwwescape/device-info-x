package com.wwwescape.deviceinfox.data.camera

enum class CameraFacing {
    FRONT,
    BACK,
    EXTERNAL,
    UNKNOWN,
}

enum class CameraHardwareLevel {
    LEGACY,
    LIMITED,
    FULL,
    LEVEL_3,
    EXTERNAL,
    UNKNOWN,
}
